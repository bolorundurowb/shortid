using System;

namespace shortid
{
    public class ShortId
    {
        private static Random _random = new Random();
        
        public static string Generate()
        {
            return String.Empty;
        }
    }
}